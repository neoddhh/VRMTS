# 🎯 多靶子关卡统计系统使用指南

## 📋 系统概述

实现了 Unity 多靶子识别和教官端关卡统计功能，支持：
- ✅ 多个靶子独立识别
- ✅ 靶子所属关卡标记（L1、L2、L3...）
- ✅ 教官端实时显示击中的靶子和环数
- ✅ 按关卡分别统计成绩
- ✅ **全新训练界面** - VR 画面 + 可折叠控制面板

---

## 🎨 新版教官端界面

### 界面布局

```
┌──────────────────────────────────────────────────┐
│  [控制面板]  │    VR 实时画面    │  [统计面板]  │
│   (可折叠)   │                   │   (可折叠)   │
│              │                   │              │
│   ◀ 心率     │                   │   靶盘  ▶   │
│   ◀ 模式     │    Unity VR       │   评分  ▶   │
│   ◀ 日志     │                   │   关卡  ▶   │
└──────────────────────────────────────────────────┘
```

### 功能特点

1. **中央 VR 画面** - 全屏显示 Unity VR 实时画面
2. **左侧控制面板** - 心率、模式控制、操作按钮
3. **右侧统计面板** - 靶盘、评分、关卡统计
4. **折叠功能** - 点击箭头收起面板，不遮挡 VR 画面

---

## 🎮 Unity 端配置

### 1. 为每个靶子配置 ShootingTarget 组件

在 Unity 中选择靶子 GameObject，在 Inspector 面板中设置：

**靶子配置示例：**

```
靶子 1 (近距离 - L1 关卡)
━━━━━━━━━━━━━━━━━━━━━
Max Radius: 0.5
Target Id: Target_1
Level Id: L1
Target Name: 1号靶
```

```
靶子 2 (中距离 - L2 关卡)
━━━━━━━━━━━━━━━━━━━━━
Max Radius: 0.5
Target Id: Target_2
Level Id: L2
Target Name: 2号靶
```

```
靶子 3 (远距离 - L3 关卡)
━━━━━━━━━━━━━━━━━━━━━
Max Radius: 0.5
Target Id: Target_3
Level Id: L3
Target Name: 3号靶
```

### 2. 字段说明

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| **Target Id** | string | 靶子唯一标识符 | Target_1, Target_2 |
| **Level Id** | string | 所属关卡编号 | L1, L2, L3 |
| **Target Name** | string | 显示名称（中文） | 1号靶, 远距靶 |

---

## 📊 教官端显示

### 1. 实时击发日志

```
🎯 学员 张三 击中 1号靶 (L1) 9.8环
🎯 学员 张三 击中 2号靶 (L2) 8.5环
🎯 学员 张三 击中 3号靶 (L3) 7.2环
```

### 2. 关卡统计面板

每个关卡独立显示统计信息：

```
┌─────────────────────────┐
│ L1              5发     │
├─────────────────────────┤
│ 平均环数      9.2环     │
│ 精准度        92%       │
│ 10环数        2发       │
│ 9环数         3发       │
└─────────────────────────┘
```

### 3. 数据结构

每次射击记录包含：
- `score` - 环数（0.9-10.9）
- `x`, `y` - 归一化坐标
- `targetId` - 靶子ID
- `levelId` - 关卡ID
- `targetName` - 靶子名称
- `timestamp` - 时间戳

---

## 🚀 使用流程

### 步骤 1：Unity 场景配置

1. 打开 Unity 项目 `D:\MilitaryVR\`
2. 在场景中选择每个靶子 GameObject
3. 找到 **ShootingTarget** 组件
4. 设置 **Target Id**、**Level Id**、**Target Name**
5. 保存场景

### 步骤 2：启动教官端

1. 双击运行：
   ```
   D:\MilitaryVR_202608050333\InstructorApp\dist2\InstructorDashboard.exe
   ```

2. 选择 **localhost** 连接模式
3. 选择 **Solo Training**

### 步骤 3：Unity 运行测试

1. Unity 编辑器点击 **Play ▶️**
2. VR 设备进入场景
3. 射击不同的靶子

### 步骤 4：查看统计

教官端会自动显示：
- ✅ 实时击发日志（显示靶子名称和关卡）
- ✅ 靶盘弹着点分布
- ✅ 关卡统计面板（L1、L2、L3 分别统计）

---

## 📈 关卡统计说明

### 统计指标

每个关卡独立计算：

1. **击发数量** - 该关卡总共击发次数
2. **平均环数** - 该关卡所有射击的平均环数
3. **精准度** - (平均环数 / 10.0) × 100%
4. **10环数** - 10环及以上的射击次数
5. **9环数** - 9-10环之间的射击次数

### 关卡颜色标识

- **L1** - 🔴 红色（近距离/放松训练）
- **L2** - 🟢 绿色（中距离/标准训练）
- **L3** - 🔵 蓝色（远距离/高级训练）

---

## 🎯 典型应用场景

### 场景 1：渐进式训练

```
L1 (15米) → 熟悉枪支操作
L2 (50米) → 标准射击训练
L3 (100米) → 远距离精准射击
```

学员依次完成 L1、L2、L3，教官端分别统计每个阶段成绩。

### 场景 2：多靶同时训练

```
场景中同时放置 3 个靶子：
- 左侧靶 (L1)
- 中央靶 (L2)
- 右侧靶 (L3)
```

学员可以自由选择射击目标，教官端自动识别并分类统计。

### 场景 3：压力测试

```
近靶 (L1) - 10发热身
远靶 (L3) - 10发压力测试
```

对比 L1 和 L3 的成绩差异，评估学员的抗压能力。

---

## 🔧 技术细节

### Unity → 教官端通信

**WebSocket 消息格式：**

```json
{
  "type": "SHOOT_SCORE",
  "data": {
    "studentId": "S2026010001",
    "score": 9.8,
    "x": 0.12,
    "y": -0.05,
    "bpm": 78,
    "targetId": "Target_1",
    "levelId": "L1",
    "targetName": "1号靶",
    "timestamp": 1723689123456
  }
}
```

### 数据存储结构

教官端 `student` 对象新增字段：

```javascript
student.levelStats = {
  "L1": {
    hits: [{score, targetId, targetName, timestamp}, ...],
    totalScore: 48.5,
    avgScore: 9.7,
    count: 5
  },
  "L2": {
    hits: [...],
    totalScore: 42.0,
    avgScore: 8.4,
    count: 5
  }
}
```

---

## ✅ 验证清单

### Unity 端检查

- [ ] 每个靶子都有 `ShootingTarget` 组件
- [ ] `Target Id` 设置且唯一
- [ ] `Level Id` 设置正确（L1/L2/L3）
- [ ] `Target Name` 设置为中文名称
- [ ] 场景中有 `PicoWebSocketClient` 组件
- [ ] WebSocket 连接正常（Console 显示连接成功）

### 教官端检查

- [ ] 启动后显示 "连接模式选择" 界面
- [ ] 选择 localhost 后进入训练页面
- [ ] 页面显示 "关卡统计" 区域
- [ ] Unity 射击后，日志显示靶子名称
- [ ] 关卡统计自动更新

---

## 🐛 常见问题

### Q1: 教官端没有显示靶子名称

**解决方案：**
1. 检查 Unity 中 `ShootingTarget` 组件的字段是否正确填写
2. 重新 Play Unity 场景
3. 查看 Unity Console 是否有 WebSocket 连接错误

### Q2: 关卡统计不更新

**解决方案：**
1. 确认已经击中靶子（Unity Console 显示击中日志）
2. 刷新教官端（重新打开监控学员）
3. 检查浏览器控制台是否有 JavaScript 错误

### Q3: 所有靶子都显示在 L1

**解决方案：**
1. 检查每个靶子的 `Level Id` 字段
2. 确保不同靶子使用不同的关卡ID（L1、L2、L3）
3. 保存 Unity 场景后重新运行

---

## 📞 技术支持

如有问题，请检查：
1. Unity Console 日志
2. 教官端浏览器 DevTools Console
3. WebSocket 连接状态（ws://127.0.0.1:5180）

---

## 🎉 完成！

现在你的系统已经支持：
- ✅ 多靶子识别
- ✅ 关卡分类统计
- ✅ 实时显示靶子名称
- ✅ 独立关卡成绩评估

开始训练吧！🎯
